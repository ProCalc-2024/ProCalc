name: Deploy Application

on: [push]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Use secret in a script
      run: echo "Using secret password"
      env:
        MY_SECRET_PASSWORD: ${{ secrets.MY_SECRET_PASSWORD }}
